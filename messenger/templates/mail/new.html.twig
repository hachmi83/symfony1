{% extends 'content_base.html.twig' %}


{% block content_body %}
    <h1>Create new Artcile !</h1>
    {{ form_start(userForm) }}
    {{ form_row(userForm) }}
    <button type="submit" class="btn btn-primary">Create</button>

    {{ form_end(userForm) }}
{% endblock %}